# AGENTS

本仓库是 MoonBit Markdown to HTML 工具项目。

- 优先保持提交粒度小，逐步累加功能。
- 功能、行为、CLI 用法或支持语法发生变化时，同步更新 `README.md`；README 更新视为对应代码变更的完成条件之一。
- 运行验证时优先使用 `/home/loviya/.moon/bin/moon check` 和 `/home/loviya/.moon/bin/moon test`。
- `resources/` 是本地参考资料目录，已被 `.gitignore` 忽略。
